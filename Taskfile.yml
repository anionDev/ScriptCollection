version: '3'

tasks:

  BaseBuildAllCodeunits:
    desc: "Base: Build all codeunits"
    silent: true
    dir: "."
    cmds:
      - "scbuildcodeunits"
    aliases:
      - basebuildallcodeunits
      - bb

  BaseBuildAllCodeunitsContainerized:
    desc: "Base: Build all codeunits containerized"
    silent: true
    dir: "."
    cmds:
      - "scbuildcodeunitsc"
    aliases:
      - basebuildallcodeunitscontainerized
      - bd

  BaseUpdateDependencies:
    desc: "Base: Update dependencies"
    silent: true
    dir: "."
    cmds:
      - "scupdatedependencies"
    aliases:
      - baseupdatedependencies
      - bud
