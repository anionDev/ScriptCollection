git reset --soft HEAD~3 //Removes the last 3 commits. The changes of these commits will be preserved as staged changed after executing this line
git commit -m "developed feature xy"
git push remotename branchname --force //force to push the branch with the squashed commits to the remote